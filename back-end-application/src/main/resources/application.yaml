spring:
  application:
    name: application
  datasource:
    url: jdbc:postgresql://172.168.20.240:5432/test_processing
    username: suffixit01
    password: SilTest@321!
  jpa:
    hibernate:
      ddl-auto: update
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  region: ${MINIO_REGION:us-east-1}
  bucket: ${MINIO_BUCKET:idp-bucket}
  accessKey: ${MINIO_ACCESS_KEY:minioadmin}
  secretKey: ${MINIO_SECRET_KEY:minioadmin}

rabbitmq:
  host: ${RABBIT_HOST:localhost}
  port: ${RABBIT_PORT:5672}
  username: ${RABBIT_USER:guest}
  password: ${RABBIT_PASS:guest}
  exchange: ${RABBIT_EXCHANGE:idp-exchange}
  routingKey: ${RABBIT_ROUTING_KEY:document.processing}

llm:
  python-proxy-url: ${PYTHON_PROXY:http://localhost:8000}

security:
  jwt:
    secret: ${JWT_SECRET:change_me_to_a_long_random_string}

management:
  endpoints:
    web:
      exposure:
        include: '*'
  prometheus:
    metrics:
      export:
        enabled: true
server:
  port: 8070
